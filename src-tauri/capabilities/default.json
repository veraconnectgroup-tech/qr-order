{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window — remote dashboard with native notifications",
  "windows": ["main"],
  "remote": {
    "urls": ["https://qr-order-iota.vercel.app"]
  },
  "permissions": ["core:default", "notification:default"]
}
