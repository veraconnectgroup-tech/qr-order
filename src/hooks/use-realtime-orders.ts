"use client";

import type { OrderWithDetails } from "@/types";

/** Step 2+: wire to DashboardProvider realtime subscriptions */
export function useRealtimeOrders(_locationId?: string): OrderWithDetails[] {
  return [];
}
