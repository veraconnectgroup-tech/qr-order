"use client";

import { DenisOsShell } from "@/components/landing/denis-os-shell";

/** Homepage — Denis operating environment, not a marketing landing page. */
export function LandingPage() {
  return <DenisOsShell />;
}
